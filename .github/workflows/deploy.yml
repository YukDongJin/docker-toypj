name: CI/CD to Amazon ECS via ECR

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: dockertoypj
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🛠️ Building Docker image with latest code.."
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./nodejs_ecr
          echo "🚀 Pushing image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG



      - name: Register new ECS task definition
        env:
          AWS_REGION: ap-northeast-2
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: dockertoypj
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "📦 Fetching current Task Definition..."
          FAMILY_NAME="dockertoypj-nodejs-task"  # <- Task Definition 이름
          echo "Task Family detected: $FAMILY_NAME"

          TASK_DEF=$(aws ecs describe-task-definition --task-definition $FAMILY_NAME)
          NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
          '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.status, .revision, .taskDefinitionArn, .requiresAttributes, .compatibilities, .registeredBy, .registeredAt)')
          echo "$NEW_TASK_DEF" > new-task-def.json

          echo "✅  Registering new Task Definition with image: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
          aws ecs register-task-definition --cli-input-json file://new-task-def.json


      - name: Update ECS service to use new task definition
        run: |
          echo "♻️ Updating ECS service to new task definition..."
          NEW_TASK_FAMILY=$(jq -r '.family' new-task-def.json)
          NEW_REVISION=$(aws ecs describe-task-definition --task-definition $NEW_TASK_FAMILY | jq -r '.taskDefinition.revision')
          aws ecs update-service \
            --cluster dockertoypj-cs \
            --service dockertoypj-nodejs-task-service-zmqxk74h \
            --task-definition ${NEW_TASK_FAMILY}:${NEW_REVISION} \
            --force-new-deployment \
            --region ap-northeast-2
          echo "✅ ECS service updated successfully!"
